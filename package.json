{
  "name": "magnetic-storm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode=development",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "build": "tsc && vite build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@yandex/ymaps3-types": "^1.0.16076882",
    "gh-pages": "^6.3.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}